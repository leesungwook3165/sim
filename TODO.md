# TODO

## 1. 클립할 일본 국토 / 한국 국토 파일 찾기
AIS 구간이 육지를 관통하는지 판정할 마스크를 한·일 전 구간으로 넓히기.

- 지금 있는 것: `tsushima_land.js` (쓰시마만, 2.1KB)
  - 출처는 저장소의 `N13-24_5129.geojson` — 일본 국토수치정보 **도로** 데이터(5.7MB).
    도로가 지나는 300m 격자를 육지로 잡고, 바다 쪽에서 flood fill로 내륙을 메워 만듦.
  - 한계: 쓰시마만 덮음. 해안에서 도로가 400m 이상 떨어진 곳은 바다로 읽힘.
- 필요한 데이터
  - 일본: 국토수치정보 **N03 행정구역**(폴리곤) 또는 **C23 해안선**, 방파제는 **C02 항만**
  - 한국: 국가공간정보포털 **해안선 / 행정경계**, 항만시설
  - 대안: Natural Earth 10m land polygons (한·일 동시 커버, 별도 인증 없음)
- 넣는 방법: 부산·하카타·쓰시마 주변만 잘라서 넣기. 같은 파이프라인(격자 → RLE+base64)이면
  각 구역 1~3KB, 대한해협 전체를 600m로 덮어도 10KB 안팎.

## 2. 보간 관련 드롭다운은 체크박스 하나로 바꾸기
- 드롭다운은 이미 없앴고 현재는 체크박스 **두 개**(`✏ 보정일만`, `⚠ 고칠 날만`).
  → 하나로 합칠지, 둘 유지할지 결정 필요.
  (둘로 둔 이유: 함께 켜면 "보정했는데 아직 관통이 남은 날"을 뽑을 수 있어서)

## 3. 그래프에서 보간한 영역 표시하기
- 속도 그래프(`_aisDrawSpeedChart`)에서 보정된 구간이 어디인지 보이게 하기.
- **제약**: 지금 선 색은 운항속도 구분(정박/저속/중속/순항)이라 그대로 둬야 함.
  → 색이 아닌 다른 채널로 표시할 방법 찾기.
    후보: 해당 시간대 배경 음영, 하단에 별도 띠(보정 구간 막대), 상단 작은 ✏ 마커,
    빗금 패턴 오버레이 등.

## 4. 출발·도착 정보는 각 항구 POI 최근접점 기준으로 — ✅ 완료
- `_aisVoyageTimes(pts, terminals)` 로 함수화. `_aisDaySummary`와 기준일 판정이 같이 쓴다.
- 방식: **그 항구에 가장 가까운 정박**을 항구 좌표로 고른 뒤 그 정박의 끝(출항)·시작(도착)을 씀.
  정박이 안 잡히면 최근접점(밴드 = 최근접거리 + 0.4km) 시각으로 대체.
- 검증(카멜리아 공식 시간표 대조): 하카타 11:59 출항(공식 12:30) → 부산 도착 후
  회항정박 4h47m(공식 4h30m) → 하카타 05:40 도착(공식 06:00). 퀸비틀 출항편 3h38m(공식 3h40m).
- 세 선박 기대편도 211~213km로 수렴 (공식 213km).

---

## 남은 문제

### A. 비틀호는 하루 여러 왕복이라 왕복 판정이 안 맞음
- `_aisSplitLegs`는 "시작점에서 가장 먼 지점" 한 곳에서만 자른다. 하루 2왕복 이상이면
  회항 정박을 못 가르고 밤새 정박이 복항편에 통째로 들어간다(복항편 17시간 같은 값).
- 그 결과 비틀호 ★기준일이 **1일**뿐 (퀸비틀 141일, 카멜리아 2698일).
- 증거: 비틀호 후보일 시간비율이 이봉분포 — q10=1.40(진짜 1왕복) vs q50=3.86(정박 섞임).
  다른 두 척은 1.3~1.5로 단봉.
- 고칠 방향: 정박을 기준으로 하루를 여러 항차로 쪼갠 뒤 항차별로 판정하기
  (지금은 "하루 = 한 항차" 가정).

### B. 기준일 판정에 쓰는 시간 기준 (참고)
- 실제 항해시간 ÷ 이론 소요시간(거리÷순항속도) = 배율. 깨끗한 날은 세 척 모두 1.3~1.4대.
  (선박 특성이 아니라 "항해 중 저속 구간 비중"이라는 물리적 성질)
- 오염(정박 섞임·AIS 공백)은 시간을 늘리기만 하므로 중앙값이 아니라
  **하위 10% 분위수**를 그 선박의 깨끗한 리듬으로 삼고 ×0.85~×1.20 안만 인정한다.
- 상수: `AIS_TIME_BASE_Q`, `AIS_TIME_DEV_LO/HI`, `AIS_REF_MIN_DAYS`

### C. 편도 운항 선박은 ★기준일 판정 밖에 있음
- 성희호·하마유호는 부산 ↔ 시모노세키 **야간 편도** 페리다(저녁 출항 → 다음날 아침 도착).
  하루에 한 방향만 가므로 `isRound`(시작·끝이 같은 터미널)가 거의 성립하지 않는다.
- 그 결과 ★기준일이 각각 **2일**뿐 (운항일 4918일 / 4712일).
  터미널·순항속도·기대편도(223km / 226km)는 제대로 자동 검출됨.
- 고칠 방향: 기준일 조건을 왕복 전용에서 떼어내고 "편도 기준일"을 따로 인정하기
  (출발항·도착항이 서로 다른 터미널이고 양쪽 가감속이 온전한 날).

### D. 산타모니카 제주 항로 — 하루 2왕복 + 경유지 유무가 섞임
- 사용자 확인 사항: 제주 항로를 **하루 2회 왕복**하는 경우가 많고,
  **추자도를 경유할 때도 있고 안 할 때도 있다**.
- 지금 결과: 왕복 판정 22일/47일, ★기준일 1일, 그 1일도 회항정박 13h15m·항해 1h34m로 엉망.
  A와 같은 원인(하루 = 한 항차 가정) + 경유 유무에 따라 편도거리가 달라지는 문제가 겹친다.
- 경유 유무는 추자도 기항 여부(`_aisPortCalls`)로 갈라낼 수 있으므로,
  기대거리를 "경유편/직항편" 두 갈래로 나눠 잡아야 한다.

### 항구 등록 관련
- 제주 항로 항구 3곳 추가(진도 서망항·추자도·제주항). 좌표는 AIS에서 자동 검출된
  터미널 중심점을 그대로 썼다.
- 터미널 이름 반경을 25km → 10km(`AIS_TERMINAL_NAME_KM`)로 좁혔다. 25km에서는
  항로 중간 지점이 "추자도", 쓰시마 북쪽 해상이 "히타카츠"로 잘못 붙었다.
- 아직 이름 없는 터미널: 하마유 34.2363,132.7119(242회, 세토내해 쪽) 등.

### E. 팬스타 미라클호 — 한 항차가 달력일 이틀에 걸친다
- 부산 ↔ 오사카. **308항차 전부**가 이틀에 걸쳐 있다(측정치).
  편도 평균 **18.5시간 · 694km** — 부산 17시경 출항 → 오사카 익일 10시경 도착.
- 지금 구조는 "하루 = 한 항차"라서 한 날에 항차의 일부만 담긴다.
  그 탓에 기대편도가 **54km**(실제 694km), ★기준일 0일로 나온다.
  터미널(부산항·오사카항)과 순항속도(23.8kt)는 정상 검출됨.
- A·C와 뿌리가 같지만 방향이 반대다.
    A(비틀호)  하루에 항차가 여러 개
    C(성희·하마유) 하루에 편도 하나 — 왕복 판정이 안 됨
    E(미라클)  항차 하나가 여러 날 — 하루 안에 안 들어감
- 결국 **"하루" 대신 "항차"를 기본 단위로** 삼아야 세 경우가 한 번에 풀린다.
  항차 = 항구 정박과 정박 사이 구간. 날짜는 항차의 출항일로 붙이면 된다.
  (`_aisPortCalls`로 정박을 이미 잡고 있으므로 그 경계로 자르면 된다)


  쓰시마링크호 가장 완벽할날이 23년 2월 27일 데이터인가겉다. 출항부터 도착 회항까지 가장 데이터가 깔끔하네  23년 3월 18, 19일
  히타카츠 경유 데이터는 26년 7월 20일 