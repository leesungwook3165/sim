# AIS 원본 다운로드 묶음 — 여기서 뽑아낸 *_track-data-*.csv 만 저장소에 둔다
route/

# Claude Code 로컬 설정 — 이 PC 경로가 박혀 있어 공유할 성격이 아니다
.claude/

# OS·에디터 부산물
Thumbs.db
desktop.ini
.DS_Store
*.swp

# 아이콘 생성(build_icons.js)에만 쓰는 개발 의존성 — 배포에는 필요 없다
node_modules/
package-lock.json
